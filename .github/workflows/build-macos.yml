name: Build macOS

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
        description: 'Application version'
      build-type:
        required: false
        type: string
        default: 'production'
        description: 'Build type (dev, staging, production)'
    outputs:
      artifact-name:
        description: "Name of the uploaded artifact"
        value: ${{ jobs.build-macos.outputs.artifact-name }}
    secrets:
      # Support both individual certificates (preferred) and combined P12 (legacy)
      APPLE_DEVELOPER_ID_APPLICATION_CERT:
        required: false
      APPLE_DEVELOPER_ID_INSTALLER_CERT:
        required: false
      APPLE_CERT_PASSWORD:
        required: false
      # GitHub workflow standard format (alternative)
      APPLE_CERTIFICATE_P12:
        required: false
      APPLE_CERTIFICATE_PASSWORD:
        required: false
      # Required for notarization
      APPLE_ID:
        required: true
      APPLE_ID_PASSWORD:
        required: true
      APPLE_TEAM_ID:
        required: true
  workflow_dispatch:
    inputs:
      build-type:
        description: 'Build type'
        required: false
        type: choice
        options:
          - dev
          - staging
          - production
        default: 'dev'

jobs:
  build-macos:
    name: Build macOS Apps (Signed & Notarized)
    runs-on: macos-14
    timeout-minutes: 90
    outputs:
      artifact-name: ${{ steps.upload.outputs.artifact-name }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: 'pip'
          cache-dependency-path: |
            requirements.txt
            r2midi_client/requirements.txt

      - name: Extract version information
        id: version
        shell: bash
        run: |
          ./.github/scripts/extract-version.sh "${{ inputs.version }}"

      - name: Validate build environment
        shell: bash
        run: |
          ./.github/scripts/validate-build-environment.sh macos

      - name: Install system dependencies
        shell: bash
        run: |
          ./.github/scripts/install-system-dependencies.sh macos

      - name: Install Python dependencies
        shell: bash
        run: |
          ./.github/scripts/install-python-dependencies.sh production

      - name: Setup Apple Developer certificates
        shell: bash
        env:
          # Support both certificate formats
          APPLE_DEVELOPER_ID_APPLICATION_CERT: ${{ secrets.APPLE_DEVELOPER_ID_APPLICATION_CERT }}
          APPLE_DEVELOPER_ID_INSTALLER_CERT: ${{ secrets.APPLE_DEVELOPER_ID_INSTALLER_CERT }}
          APPLE_CERT_PASSWORD: ${{ secrets.APPLE_CERT_PASSWORD }}
          APPLE_CERTIFICATE_P12: ${{ secrets.APPLE_CERTIFICATE_P12 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          ./.github/scripts/setup-certificates.sh

      - name: Build applications
        shell: bash
        run: |
          ./.github/scripts/build-briefcase-apps.sh macos signed

      - name: Create signed and notarized PKG installers
        shell: bash
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          ./.github/scripts/create-macos-pkg.sh \
            "${{ steps.version.outputs.version }}" \
            "${{ inputs.build-type }}" \
            "${{ secrets.APPLE_ID }}" \
            "${{ secrets.APPLE_ID_PASSWORD }}" \
            "${{ secrets.APPLE_TEAM_ID }}"

      - name: Organize final artifacts
        shell: bash
        run: |
          echo "📦 Organizing final PKG installers..."
          
          # The create-macos-pkg.sh script already creates the final packages
          # Just verify and display what was created
          
          echo "✅ PKG Installers created:"
          find artifacts/ -name "*.pkg" | while read pkg; do
            if [ -f "$pkg" ]; then
              size=$(du -h "$pkg" | cut -f1)
              echo "  📦 $(basename "$pkg") ($size)"
            fi
          done
          
          echo ""
          echo "📍 Optional DMG files:"
          find artifacts/ -name "*.dmg" | while read dmg; do
            if [ -f "$dmg" ]; then
              size=$(du -h "$dmg" | cut -f1)
              echo "  💿 $(basename "$dmg") ($size)"
            fi
          done
          
          echo ""
          echo "✅ All artifacts ready for distribution"

      - name: Validate PKG installers
        shell: bash
        run: |
          echo "🔍 Validating PKG installers (primary distribution format)..."
          
          pkg_count=0
          find artifacts/ -name "*.pkg" | while read file; do
            if [ -f "$file" ]; then
              pkg_count=$((pkg_count + 1))
              echo "Validating PKG: $(basename "$file")"
              
              # Check PKG signature
              if pkgutil --check-signature "$file" 2>/dev/null | grep -q "signed"; then
                echo "  ✅ PKG signature valid"
              else
                echo "  ❌ PKG signature invalid"
              fi
              
              # Check Gatekeeper compatibility for installers
              if spctl --assess --type install "$file" 2>/dev/null; then
                echo "  ✅ PKG passes Gatekeeper assessment"
              else
                echo "  ⚠️ PKG Gatekeeper assessment failed (may be normal before distribution)"
              fi
              
              # Check notarization stapling
              if xcrun stapler validate "$file" 2>/dev/null; then
                echo "  ✅ Notarization ticket stapled"
              else
                echo "  ⚠️ No notarization ticket found"
              fi
              
              echo ""
            fi
          done
          
          echo "📍 Optional DMG validation..."
          find artifacts/ -name "*.dmg" | while read file; do
            if [ -f "$file" ]; then
              echo "Validating DMG: $(basename "$file")"
              
              # Check DMG signature
              if codesign --verify --deep --strict "$file" 2>/dev/null; then
                echo "  ✅ DMG signature valid"
              else
                echo "  ⚠️ DMG signature verification failed"
              fi
              
              echo ""
            fi
          done
          
          echo "✅ PKG installer validation complete"

      - name: Upload artifacts
        id: upload
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ inputs.build-type }}-${{ github.run_number }}
          path: artifacts/
          retention-days: ${{ inputs.build-type == 'production' && 90 || 30 }}
          compression-level: 6

      - name: Build summary
        shell: bash
        run: |
          echo "✅ macOS PKG installer build complete!"
          echo ""
          echo "📦 PRIMARY DISTRIBUTION FILES (PKG Installers):"
          find artifacts/ -name "*.pkg" | while read pkg; do
            if [ -f "$pkg" ]; then
              size=$(du -h "$pkg" | cut -f1)
              echo "  📦 $(basename "$pkg") ($size) - READY FOR DISTRIBUTION"
            fi
          done
          
          echo ""
          echo "💿 OPTIONAL FILES (DMG Images):"
          find artifacts/ -name "*.dmg" | while read dmg; do
            if [ -f "$dmg" ]; then
              size=$(du -h "$dmg" | cut -f1)
              echo "  💿 $(basename "$dmg") ($size) - Manual installation option"
            fi
          done
          
          echo ""
          echo "📋 Distribution Status:"
          echo "  ✅ PKG installers are signed with Developer ID Installer"
          echo "  ✅ PKG installers are notarized by Apple"
          echo "  ✅ PKG installers will install to /Applications automatically"
          echo "  ✅ No security warnings will appear to users"
          echo "  ✅ Ready for public distribution"
          
          # Generate traditional build summary for compatibility
          ./.github/scripts/generate-build-summary.sh macos "${{ inputs.build-type }}" "${{ steps.version.outputs.version }}" pkg-focused

      - name: Cleanup signing materials
        if: always()
        shell: bash
        run: |
          echo "🧹 Cleaning up signing materials..."
          
          # Remove temporary certificates
          rm -f *.p12 2>/dev/null || true
          
          # Remove temporary keychain
          if [ -n "${TEMP_KEYCHAIN:-}" ]; then
            security delete-keychain "$TEMP_KEYCHAIN" 2>/dev/null || true
          fi
          
          # Remove signing identities file
          rm -f /tmp/signing_identities.sh 2>/dev/null || true
          
          # List remaining keychains for verification
          echo "Remaining keychains:"
          security list-keychains -d user
          
          echo "✅ Cleanup complete"
